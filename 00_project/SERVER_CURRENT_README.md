# 服务器当前版本入口

当前版本：`V1.0`；技术路线已冻结，必要数据收集与质量审核进行中。

- `../versions/v1/V1_研究设计与数据需求.md`：V1研究设计；
- `../versions/v1/V1_最小必要数据与下载方案.md`：V1最小下载方案；
- `../manifests/v1/`：V1数据清单和官方来源登记；
- `../logs/v1_active/`：当前下载日志；
- `../logs/archive/`：已结束或被替代的历史日志。

服务器数据主题目录 `01_pv` 至 `10_policy`、`downloads/partial` 和 `scripts` 是运行路径，不随文档版本移动。
早期 `legacy_pre_v1` 技术路线已登记后删除，避免被误认为当前方法。
