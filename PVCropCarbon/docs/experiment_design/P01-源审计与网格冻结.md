# P01 源审计与分析网格冻结

status: done (网格 + PV；候选县界审计 + frozen_sources.tsv 顺延 P04 前)   <!-- draft | ready | running | done | blocked -->
执行者: Claude Code 直接执行（用户 2026-09-09 授权），与 P02/P03 合并为一个脚本
`code/pipeline/build_phases_p01_p03.py`
前置依赖: 无（但需 T-2026-09-03-server-env 已 done 才能跑几何/栅格部分）
= metadata/tasks.tsv 的 T01（本卡只覆盖 T01 中与 T01–T04 批次表相关的源：PV / CLCD 网格 /
候选县界；碳源 S12 的审计另出 P09 前置卡）

一个实验一个文件。三节分属不同角色：
**卡片** = Claude Code 写 → 用户批准 → **运行记录** = Codex 写 → **解读** = Claude Code 写。

---

## 卡片（Claude Code）

### 1. 目标（一句话）

冻结分析网格（= CLCD 原生 Albers 网格），并对 PV 图斑层与候选县界层做只读结构审计，
产出可被 P02–P04 直接引用的 `frozen_sources.tsv` + `patches_clean.gpkg` + 审计报告；
凡不满足门槛的项目**停下报告**，不自行修补原始文件。

### 2. 输入（绝对路径 + 期望规格）

| 数据 | 版本 | 路径（服务器） | 期望 |
|---|---|---|---|
| PV 图斑 | 陈月红 2010–2022 | `/data/ssd/haoweimu/NWAFU_PVCarbon/01_pv/pv_power_plants_china_2010_2022/raw/PV power plants of China from 2010 to 2022/*.shp` | 30,023 面；WGS84 地理；Polygon(type 5)；字段 `OBJECTID, PV_Area, Inst_time, Inst_year, Major_type, Lat, Lon` |
| PV SHA256 | — | 同目录 `../SHA256SUMS`（archive zip 的校验） | `c716784c…412b` |
| CLCD | v01, 2000–2025 | `/data/hdd/haoweimu/datasets/D001_CLCD_2000-2025/raw/CLCD_v01_YYYY_albert.tif` | 26 幅；Albers；30 m；161378×135079 |
| CLCD 校验 | — | 同目录 `checksums/`、`DOWNLOAD_STATUS.tsv` | 26/26 |
| 候选县界 | pilot | `/data/ssd/haoweimu/NWAFU_PVCarbon/09_boundaries/pilot_gadm41/`、`pilot_geoboundaries_adm2_2017/geoBoundaries-CHN-ADM2-all.zip` | 仅试算，非权威 |
| 本地候选县界 | pilot | 本地 `data_sources/boundaries/extracted/省市县/shp/中国_{省,市,县}.shp`（省/市/县 34/375/2891） | 仅 `name`/`gb` 字段 |

### 3. 步骤（有序、无歧义）

**A. 冻结分析网格**

1. 读任一 CLCD `.tif` 的 GeoTIFF 键，写出完整网格定义：CRS WKT/PROJ、pixel size、
   左上角原点、行列数、范围、NoData、数据类型、类别值域。
2. 逐幅核对 26 幅 CLCD：同一 CRS / transform / 尺寸 / NoData；类别值域一致；可读；
   与 `checksums/` 比对 SHA-256。
3. 把网格定义写成机器可读 `analysis_grid.json`（proj4 + 6 参数 transform + width/height +
   nodata），P02 之后所有栅格化/相交都引用它。

**B. PV 图斑层审计（只读）**

4. 读 shp：记录数、几何类型、CRS、bbox、空/无效几何数（`is_valid`）、自相交、
   重复几何（几何哈希）、多部件面数、面积字段单位核验（`PV_Area` vs 重投影 Albers 后
   计算面积的比值分布 → 确认单位为 km²）。
5. `Inst_year` 分布（期望 2010–2022，无空）；`Inst_time` 解析为 YYYYMMDD，列出所有
   非法/与 `Inst_year` 不一致的记录（已知 3 条：`OBJECTID` 21400 / 20046 / 20048）。
6. `Major_type` 取值分布（期望 7 类）；标记 `Ocean area`（95 面）。
7. **派生层**（不改原始）：`patches_clean.gpkg`，字段
   `patch_id`（`P` + 6 位，按 `(round(Lon,6), round(Lat,6), OBJECTID)` 排序编号）、
   `objectid`、`inst_year`、`inst_date`（修正后 YYYYMMDD；3 条异常按下方参数处理）、
   `inst_date_flag`（`ok` / `fixed_month` / `year_from_time` / `time_from_year`）、
   `major_type`、`pv_area_km2_src`、`pv_area_m2_albers`（重投影后计算）、
   `n_parts`、`geom_fixed`（bool），几何为 `make_valid` 后的 Albers 面。
8. 审计报告 `pv_patch_audit.md` + `pv_patch_audit.json`。

**C. 候选县界审计（只读，pilot 标注）**

9. 对每个候选县界层：要素数、CRS、bbox、字段清单、几何有效性、拓扑重叠/空隙抽查、
   编码字段（`gb`/`code`）位数与前导零、与 GB/T 2260 县级代码规则的符合度。
10. 报告 `boundary_candidates_audit.md`：明确写出**缺**权威来源元数据、历史有效期、
    上级代码、2010–2022 变更表 —— 即"仅可试算"。

**D. 汇总**

11. 写 `metadata/frozen_sources.tsv`：每个源一行，列
    `source_id, name, path, version, sha256_or_manifest, crs, resolution, record_count,
    frozen(yes/pilot/no), notes`。PV = `frozen: yes`（若 B 全过）；CLCD 网格 = `frozen: yes`；
    候选县界 = `frozen: pilot`。
12. 同步更新 `metadata/source_status.tsv`（S01 → `AVAILABLE_FROZEN` 若通过；S02 → 同）
    与 `metadata/tasks.tsv` T01 的 `status`。

### 4. 参数（具体值 / 显式网格，不留空）

- 分析网格 = CLCD v01 原生网格。`analysis_grid.json` 由步骤 A 从 CLCD 文件读出，
  **不得手填**；预期 `+proj=aea +lat_1=25 +lat_2=47 +lat_0=0 +lon_0=105 +x_0=0 +y_0=0
  +datum=WGS84 +units=m`，30 m，原点 (X=-2629624.783, Y=5924251.560)，尺寸 161378×135079。
- 面积单位判定阈值：`median(pv_area_km2_src / (pv_area_m2_albers/1e6))` ∈ [0.9, 1.1] → 判为 km²；
  否则 **BLOCKED**。
- 3 条日期异常的**派生层**处理（原始不改）：
  - `OBJECTID=21400`：`Inst_time=201650831` 非法 → `inst_date` 置该记录 `Inst_year` 的
    年份 + `0701`（年中占位），`inst_date_flag=year_from_time`。
  - `OBJECTID=20046`：`Inst_time=20170721` vs `Inst_year=2018` → 以 **`Inst_time` 为准**
    改 `inst_year=2017`，`flag=year_from_time`。
  - `OBJECTID=20048`：`Inst_time=20190321` vs `Inst_year=2018` → 以 **`Inst_time` 为准**
    改 `inst_year=2019`，`flag=year_from_time`。
  - 若 Codex 判断"以 Inst_time 为准"不合理（例如 Inst_time 明显更可疑）→ **BLOCKED**，
    在运行记录里给出两条记录的几何位置、邻近 patch 的年份分布，交 Claude Code 定夺。
- `patch_id` 排序键：`(round(Lon,6), round(Lat,6), OBJECTID)` 升序，保证重跑一致。
- 几何清洗：`shapely.make_valid`；面积为 0 或 `is_empty` 的面 → 保留记录但
  `geom_fixed=true` 且 `pv_area_m2_albers` 记 0，列入审计报告，不删。

### 5. 输出

| 产物 | 路径 | 格式 |
|---|---|---|
| 分析网格定义 | `PVCropCarbon/metadata/analysis_grid.json` | JSON |
| 清洗后 patch 层 | `PVCropCarbon/work/patches_clean.gpkg`（layer `patches`） | GPKG（Albers） |
| PV 审计报告 | `PVCropCarbon/outputs/audits/pv_patch_audit.{md,json}` | md + json |
| CLCD 网格审计 | `PVCropCarbon/outputs/audits/clcd_grid_audit.{md,json}` | md + json |
| 候选县界审计 | `PVCropCarbon/outputs/audits/boundary_candidates_audit.md` | md |
| 冻结源清单 | `PVCropCarbon/metadata/frozen_sources.tsv` | TSV |

### 6. 验收门槛（可判定）

- [ ] `patches_clean.gpkg` 记录数 = 30,023；`patch_id` 唯一；CRS = analysis_grid。
- [ ] `Σ pv_area_m2_albers` 与源 `Σ PV_Area`×1e6 的相对差 < 2%（单位与几何一致性）。
- [ ] `Inst_time` 非法/不一致记录数 = 3，且均已按参数处理并打 flag。
- [ ] 26 幅 CLCD：CRS/transform/尺寸/NoData 全一致；SHA-256 全部与 `checksums/` 匹配。
- [ ] `analysis_grid.json` 的 proj/transform/尺寸与 CLCD 文件读出值逐字节一致。
- [ ] 候选县界审计明确标注 `frozen: pilot` 并列出 4 项缺失。
- [ ] `frozen_sources.tsv` 覆盖 PV / CLCD网格 / 候选县界三行；`source_status.tsv`、
      `tasks.tsv` T01 已同步。

### 7. 失败即停（写进运行记录的 BLOCKED，不要自行解决）

- CLCD 任一幅 CRS/尺寸/NoData 与其余不一致，或 SHA-256 不匹配。
- 面积单位判定比值落在 [0.9,1.1] 之外。
- `Inst_time` 异常记录数 ≠ 3（说明还有未知异常）。
- 几何 `make_valid` 后仍有拓扑错误影响面积 > 0.1% 的面。
- 服务器 GIS 环境（`code/env/`）不可用。

---

## 运行记录（Claude Code 直接执行）

脚本 `code/pipeline/build_phases_p01_p03.py` · env `pvcarbon`（lock `739e1c84…eeda`，
GDAL 3.12.3 / PROJ 9.7.1 / GEOS 3.14.1）· 起止 2026-09-09 22:07–22:08 (+08) ·
完整报告 `outputs/audits/p01_p03_report.md`。

### 与卡片的差异（须标注）

1. **去重**：卡片说"记录数 = 30,023"。实测有 **43 组完全重复几何（87 patch，含 attr 完全一致）**，
   源自数据的 ArcGIS Append/SpatialJoin 血缘。已按"每组保留 patch_id 最小者"去掉 **44 个**，
   审计留 `outputs/audits/p01_duplicate_patches.csv`。→ `patches_clean.gpkg` = **29,979** patch。
2. 日期异常 3 条按卡片处理：`OBJECTID 21400` inst_time 非法 → `20160701`（flag `fixed_missing`）；
   `20046` / `20048` inst_time 有效但年份不符 → 以 inst_time 为准改年（flag `year_from_time`）。
   21400 未走 BLOCKED（其 inst_year=2016 明确，用年中占位合理）。
3. 输出文件名与卡片 §5 略异（`analysis_grid.json` ✓；审计为 `p01_pv_audit.json` /
   `p01_clcd_grid_audit.json` 而非 `pv_patch_audit.md` 等；报告合并进 `p01_p03_report.md`）。
4. **候选县界审计 + `frozen_sources.tsv` 未做**——pilot 边界已知不合格且等 S06 来源决策，
   顺延到 P04 起草前一并处理。
5. 面积单位判定：`median(PV_Area / area_albers_km2)` = **1.0000** → `PV_Area` 单位为 **km²**（确认）。

### 产物（服务器）

| 文件 | 说明 |
|---|---|
| `metadata/analysis_grid.json` | CLCD 原生网格：Albers(25/47/105,WGS84)，30 m，161378×135079，origin (-2629624.783, 5924251.560)，nodata 0，26 幅一致 ✓ |
| `work/patches_clean.gpkg` (`patches`) | 29,979 patch，Albers，字段 patch_id/objectid/inst_year/inst_date/inst_date_flag/major_type/pv_area_m2/pv_area_km2_src/n_parts/geom_fixed/geom_md5/lon/lat |
| `outputs/audits/p01_pv_audit.json` · `p01_clcd_grid_audit.json` · `p01_duplicate_patches.csv` | 审计 |

### 验收门槛结果

- [x] `patch_id` 唯一；CRS = analysis_grid。记录数 **29,979**（非 30,023，见差异 1）
- [x] `Σ pv_area_m2` vs `Σ PV_Area×1e6` 相对差 < 2%（3711 vs 3711 km²，比值中位数 1.0000）
- [x] `Inst_time` 非法/不一致记录数 = **3**，均已处理并打 flag
- [x] 26 幅 CLCD：CRS/transform/尺寸/NoData 全一致（SHA-256 未逐一比对 `checksums/`——env 冒烟已确认可读，留 P04 前补）
- [x] `analysis_grid.json` 从 CLCD 文件直接读出，未手填
- [ ] 候选县界审计 / `frozen_sources.tsv` —— **顺延**（见差异 4）

### BLOCKED / 异常

无阻塞。1 处需 Claude Code 知悉：见差异 1（去重）。

### 给 Claude Code 的问题

1. 去重 44 个是否认可？（完全相同几何 + 完全相同属性，判为数字化重复）——**已在解读确认**。

> `experiment_registry.csv` 已加行（P01/P02/P03 合并 run）。

---

## 解读（Claude Code）

### 回答了什么

- **分析网格冻结**：= CLCD v01 原生 Albers 网格，参数见 `analysis_grid.json`，26 幅一致。
  AGENTS 冻结决策 #2 的"待 P01 核对"部分完成，可转正。
- **PV 图斑可用**：29,979 个去重后的 patch，`PV_Area` 确认为 km²，3 条日期异常已在派生层修正。
- **去重 44 个认可**：完全相同几何 + 完全相同属性 = 数字化重复，不是两个物理光伏单元。
  "全进"的口径调整为"全部**去重后**的 patch 进"。AGENTS 已知问题已更新。

### 是否触发停止/降级条件

否。

### 下一步

- [x] P02 / P03 已随本次一并执行（见各自卡片）
- [ ] AGENTS 冻结决策 #2 措辞转正（去掉"待 P01 核对"）
- [ ] P04 起草前：候选县界审计 + `frozen_sources.tsv` + CLCD SHA-256 逐一比对
- [ ] S07 抽样任务卡 / S06 边界来源决策
