# E0X <实验名>

status: draft            <!-- draft | ready | running | done | blocked -->
前置依赖: E00            <!-- 这些实验须已 done -->
对应科学问题: 计划 §X.X

一个实验一个文件。三节分属不同角色：
**卡片** = Claude Code 写 → 用户批准 → **运行记录** = Codex 写 → **解读** = Claude Code 写。

---

## 卡片（Claude Code）

### 1. 目标（一句话）

<这次要判定什么，不是"跑一下 X"。>

### 2. 输入（绝对路径 + 期望规格）

| 数据 | 版本 | 路径 | 期望 CRS / 分辨率 / NoData / 单位 |
|---|---|---|---|
|  |  |  |  |

### 3. 步骤（有序、无歧义）

1.
2.

### 4. 参数（具体值 / 显式网格，不留空）

- <参数> = <值>  或  <参数> ∈ {a, b, c}

### 5. 输出

| 产物 | 路径 | 命名 | 格式 |
|---|---|---|---|
|  | `output/...` |  |  |

### 6. 验收门槛（可判定：数值阈值或布尔）

- [ ]
- [ ]

### 7. 失败即停（不要自行解决，写进运行记录的 BLOCKED）

-

---

## 运行记录（Codex）

卡片 commit: `<短哈希>`  ·  环境: `code/env/` @ sha256 `<…>`  ·  起止: `<…>`

### 实际参数（与卡片的差异必须标注）

-

### 产物

| 文件 | SHA256 | 尺寸/行数 |
|---|---|---|

### 验收门槛结果

- [x] <门槛>：通过（<数值>）
- [ ] <门槛>：**未通过** → 见 BLOCKED

### BLOCKED / 异常

<现象、涉及的条目/文件、已做处理。>

### 给 Claude Code 的问题

1.

> 完成后：`experiment_registry.csv` 加一行；本文件 status 改 done/blocked。

---

## 解读（Claude Code）

### 回答了什么

<对照卡片 §1。结论 + 关键数值 + 图。>

### 是否触发停止/降级条件

-

### 下一步

- [ ] 继续 E0X+1
- [ ] 改卡片重跑：<说明>
- [ ] 升级冻结决策（新建 versions/ 文档）：<说明>
- [ ] 更新 `INDEX.md` 状态表、`docs/paper/`
